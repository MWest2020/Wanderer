# Wanderer

Try it:

<!-- quickstart:curl -->
```sh
curl -sSL "https://example.invalid/wanderer.tar.gz" | tar xz wanderer && ./wanderer version
```

<!-- quickstart:docker -->
```sh
docker run --rm ghcr.io/mwest2020/wanderer-exapp:latest wanderer version
```

more text
