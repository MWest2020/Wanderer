# Habitat run 08 — UI answer sheet (tasks 8.1–8.4)

Contract: `openspec/changes/2026-09-19-propose-accountability-dimension/`
(specs/web-ui/spec.md, design.md "UI direction").

## Scope — ONLY these tasks
- [ ] 8.1 Accountability section on `/ui/scans/{id}/assessment`: one
  plain-language question per rule, answered ja / nee / onbekend /
  n.v.t., with a one-sentence verdict, evidence collapsed beneath
  (`<details>`, no navigation away), and exactly one remediation line
  per failing rule. ALL copy comes from
  `internal/assessor/wand/accountability_nl.yaml` (run 07) — no strings
  in the template. Rule IDs, RDAP jargon and RFC numbers only inside the
  expanded evidence. n.v.t., onbekend and nee are visually distinct.
  A rationale whose reason has subject `scanner` renders as an operator
  environment warning next to the dimension ("scanner heeft geen IPv6 —
  v6-paden niet gemeten"), never as a property of the target.
- [ ] 8.2 Overview (`/ui/`): an accountability pill per target row,
  colour matching the dimension verdict, linking to that target's report
  section; states "niet beoordeeld" (scans predating the dimension) and
  "n.v.t."; no new navigation tab (ADR-0017 keeps its two-tab IA). The
  worst-dimension score skips onbekend, n.v.t. and absent dimensions and
  states which dimensions it covers.
- [ ] 8.3 Read Wordsworth's UI for tone and interaction patterns before
  writing copy-adjacent markup (headline → expandable evidence → next
  action). Check a `.nl` fleet specifically: two of five questions are
  structurally n.v.t. there, so the other four must still carry the
  story. If they do not, say so in the run report.
- [ ] 8.4 Playwright spec under `tests/playwright/specs/` covering:
  expanding evidence, and onbekend vs n.v.t. rendering. If the browsers
  cannot be installed in the cage (no egress to the Playwright CDN),
  write the spec file, say so plainly in the run report, and do NOT
  claim it ran.

## Out of scope for this run
No assessor/probe changes, no docs (run 09).

## Done means
`go build ./...`, `go vet ./...`, `go test ./...` green (offline from
vendor/, this includes the doc-lint test that pairs a UI-touching ADR
with a Playwright spec); `openspec validate
2026-09-19-propose-accountability-dimension --strict` green. Budget is
$5 — leave room for your run report.
