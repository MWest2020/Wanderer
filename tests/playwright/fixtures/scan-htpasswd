# Playwright fixture: bcrypt htpasswd file for the `scan-dev`
# project (answer-first-ui, run 05). The door's scan input is gated
# on a signed-in user (spec.md "Submitting SHALL require a signed-in
# user"), so exercising it in Playwright needs real HTTP Basic
# credentials rather than the retired --ui-allow-scan dev flag.
# Username/password below are test-only, not a real credential.
# Plaintext password (referenced by playwright.config.ts's
# httpCredentials for this project): playwright-scan
playwright:$2a$10$JlX2s/6ebimskVOEb/JL7uuD8iRUiKJpPK5JHpyT0x5GGiRT2ARpK
